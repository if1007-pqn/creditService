/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.8/userguide/tutorial_java_projects.html
 */
buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:2.0.2.RELEASE")
    }
}

plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // Apply the application plugin to add support for building an application
    id 'application'
}

apply plugin: 'eclipse'
apply plugin: 'idea'
apply plugin: 'org.springframework.boot'
apply plugin: 'io.spring.dependency-management'

// Define the main class for the application
mainClassName = 'com.service.credit.Application'

dependencies {
    // This dependency is found on compile classpath of this component and consumers.
    compile 'com.google.guava:guava:23.0'
    compile 'io.jsonwebtoken:jjwt:0.7.0' //jwt token
    compile 'org.projectlombok:lombok:1.18.0' //getter model

    compile 'io.springfox:springfox-swagger2:2.8.0' //swagger core
    compile 'io.springfox:springfox-swagger-ui:2.7.0' //swagger page

    compile 'org.springframework.data:spring-data-mongodb' //mongo
    compile 'org.springframework.boot:spring-boot-starter-web' //spring boot
    compile 'org.springframework.boot:spring-boot-starter-actuator' //actuator
    // compile 'org.springframework.boot:spring-boot-starter-data-mongodb'
    // compile 'org.springframework.data:spring-data-commons'


    // Use JUnit test framework
    testCompile 'junit:junit:4.12'
}

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
    mavenCentral()
}
